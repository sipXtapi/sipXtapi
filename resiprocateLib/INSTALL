============================================================
               I N S T A L L A T I O N
============================================================

You must manually retrieve resiprocate code from Subversion 
repository before building reciprocateLib. 
Execute:

  svn co http://scm.sipfoundry.org/rep/resiprocate/main resiprocate

See: http://track.sipfoundry.org/browse/BBR-3

Unix : Building From Subversion
===============================

If you've obtained source from subversion, you must do this first,
then continue on with "Unix: Building from Source Code" or "Windows:
Building from Source Code"

  autoreconf --install --force


Unix : Building from Source Code
================================

  ./configure [options]

Noteworthy options:

  --help                    See list of all options

To build and install:

  make
  make check
  make install

