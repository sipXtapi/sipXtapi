<?xml version="1.0" encoding="UTF-8" ?>
<!-- $Id: build.xml,v 1.12 2001/04/19 17:55:36 starksm Exp $ -->

<project name="commserver" default="jar" basedir=".">
  <patternset id="test.classes" excludes="**"/>
  <property name="top.build.dir" value="${basedir}/../.."/>
  <property name="build.dir" value="${top.build.dir}/profilegenerator/commserver"/>

  <import file="../../ant-targets.xml"/>

  <path id="base.path">
    <!-- SIPXCONFIG -->
    <pathelement location="${common.jar}"/>

    <!-- UTIL -->
    <pathelement location="${jdom.jar}"/>
    <pathelement location="${log4j.jar}"/>

    <!-- J2EE -->
    <fileset dir="${jboss.lib}" includes="**/*.jar"/>
    <fileset dir="${jboss.container.lib}" includes="**/*.jar"/>
    <fileset dir="${tomcat.common.lib}" includes="**/*.jar"/>
  </path>
  <property name="classpath" refid="base.path" />

  <!-- J A R -->
  <target name="jar" depends="compile" description="build jar" >
    <jar jarfile="${commserver.jar}" >
     <fileset dir="${classes.dir}" >
       <include name="**/*.class" />
     </fileset>
    </jar>
  </target>

</project>

