# autogenerated profile for Cisco IP Phones
# Model $phone.Model.ModelId Maxlines $phone.Model.MaxLineCount
#foreach ($group in $phone.Settings.Values)
#if ($group.Name != "upgrade")
#foreach ($setting in $group.Values)
#if ($setting.Value != "")
${setting.ProfileName}:$!{setting.Value}
#end
#end
#end
#end
#foreach ($line in $phone.ProfileLines)
#set ($i = $velocityCount)
#foreach ($group in $line.Values)
#foreach ($setting in $group.Values)
${group.Name}${i}_${setting.ProfileName}:$!{setting.Value}
#end
#end
#end
